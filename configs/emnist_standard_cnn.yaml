seed: 42

batch_size: 128

epochs: 50

learning_rate: 0.001

weight_decay: 0.0001

num_workers: 0

num_classes: 47

model_path: models/emnist_standard_cnn_improved/best_model.pth

scheduler:
  factor: 0.5
  patience: 2
  min_lr: 0.00001

early_stopping_patience: 10